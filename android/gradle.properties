# Keep heap modest: very large -Xmx + metaspace can exhaust native memory on Windows
# during R8/compilation (mmap / G1 virtual space failures).
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
